import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: { marginTop: 16 },
  container: {
    padding: 20,
    borderRadius: 20,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },
  feeText: { fontSize: 13, lineHeight: 18, fontWeight: "400", marginBottom: 2 },
  icon: { height: 24, width: 24, position: undefined },
});
